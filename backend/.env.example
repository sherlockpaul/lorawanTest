# Copy this file to .env and fill in your values

# PostgreSQL
DATABASE_URL=postgresql://chirpstack_app:password@<proxmox-ip>:5432/lorawan_app

# MQTT (ChirpStack broker)
MQTT_HOST=<proxmox-ip>
MQTT_PORT=1883

# ChirpStack application ID (from ChirpStack web UI)
CHIRPSTACK_APP_ID=1
